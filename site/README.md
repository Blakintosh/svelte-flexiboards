# Flexiboards site

The SvelteKit docs and examples site at [flexiboards.dev](https://flexiboards.dev/). `pnpm dev` after building the packages; `pnpm build-llms-docs` and `pnpm build-registry` regenerate the Markdown twins and the registry, and both run before `dev` and `build`.
