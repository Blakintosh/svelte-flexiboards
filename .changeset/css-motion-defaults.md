---
'@flexiboards/core': minor
'@flexiboards/react': patch
'@flexiboards/svelte': patch
---

Use sine in-out reordering and circ-out drops in `cssTransitionConfig()`, with optional CSS easing tokens. Keep `simpleTransitionConfig()` deprecated with its original 150ms timing and easing.
