{
    "rewrites": [
      { "source": "/(.*)", "destination": "/site/$1" }
    ],
    "buildCommand": "cd site && pnpm install && pnpm run build",
    "outputDirectory": "site/.svelte-kit/output/client",
    "installCommand": "pnpm install"
}